{
  "name": "prism-osint-toolkit",
  "version": "1.1.0",
  "description": "The free OSINT toolkit behind PRISM (prism-tools.vip): selector enrichment via free public APIs + a 660-tool URL catalog, exposed as a zero-dependency library, CLI, and local HTTP API.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "prism": "dist/cli.js"
  },
  "main": "dist/lib.js",
  "types": "dist/lib.d.ts",
  "exports": {
    ".": {
      "types": "./dist/lib.d.ts",
      "import": "./dist/lib.js"
    }
  },
  "files": [
    "dist",
    "data",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "node --experimental-strip-types --watch src/cli.ts",
    "serve": "node --experimental-strip-types src/cli.ts serve",
    "start": "node dist/cli.js serve",
    "check": "node scripts/check-catalog.mjs",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "osint",
    "intelligence",
    "recon",
    "pivot",
    "enrichment",
    "investigation",
    "cli"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.2"
  }
}
